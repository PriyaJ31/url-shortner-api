# Copy to .env and adjust as needed
SECRET_KEY=change-me
# Leave unset to use local SQLite (urls.db).
# For Postgres: DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/shortener
# For MySQL:    DATABASE_URL=mysql+pymysql://user:pass@localhost:3306/shortener
